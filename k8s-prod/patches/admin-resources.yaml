apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: pdfdiff
spec:
  template:
    spec:
      containers:
        - name: admin
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
