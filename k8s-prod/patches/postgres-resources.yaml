apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: pdfdiff
spec:
  template:
    spec:
      containers:
        - name: postgres
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "700m"
              memory: "1Gi"
