apiVersion: apps/v1
kind: Deployment
metadata:
  name: viewer
  namespace: pdfdiff
spec:
  template:
    spec:
      containers:
        - name: viewer
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
