apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: pdfdiff
spec:
  template:
    spec:
      containers:
        - name: api
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
