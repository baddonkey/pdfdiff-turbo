version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: pdfdiff
      POSTGRES_USER: pdfdiff
      POSTGRES_PASSWORD: pdfdiff
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 15
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  api:
    build: 
      context: .
      dockerfile: ./api/Dockerfile
    image: pdfdiff-turbo-api:1.1.7
    environment:
      PROJECT_NAME: pdfdiff-turbo
      DATABASE_URL: postgresql+asyncpg://pdfdiff:pdfdiff@postgres:5432/pdfdiff
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: rpc://
      DATA_DIR: /data
      JWT_SECRET: change-me
      RENDER_DPI: "150"
    volumes:
      - data:/data
      - ./samples:/data/samples:ro
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    ports:
      - "8000:8000"
    command: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    restart: unless-stopped

  worker:
    build: 
      context: .
      dockerfile: ./api/Dockerfile
    image: pdfdiff-turbo-worker:1.1.7
    environment:
      PROJECT_NAME: pdfdiff-turbo
      DATABASE_URL: postgresql+asyncpg://pdfdiff:pdfdiff@postgres:5432/pdfdiff
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: rpc://
      DATA_DIR: /data
      JWT_SECRET: change-me
      RENDER_DPI: "150"
    volumes:
      - data:/data
      - ./samples:/data/samples:ro
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: celery -A app.core.celery_app.celery_app worker --loglevel=INFO --concurrency=40 --prefetch-multiplier=1 -Q jobs,pages
    restart: unless-stopped

  beat:
    build: 
      context: .
      dockerfile: ./api/Dockerfile
    image: pdfdiff-turbo-beat:1.1.7
    environment:
      PROJECT_NAME: pdfdiff-turbo
      DATABASE_URL: postgresql+asyncpg://pdfdiff:pdfdiff@postgres:5432/pdfdiff
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: rpc://
      DATA_DIR: /data
      JWT_SECRET: change-me
      RENDER_DPI: "150"
    volumes:
      - data:/data
      - ./samples:/data/samples:ro
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: celery -A app.core.celery_app.celery_app beat --loglevel=INFO
    restart: unless-stopped

  flower:
    build: 
      context: .
      dockerfile: ./api/Dockerfile
    image: pdfdiff-turbo-flower:1.1.7
    environment:
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: rpc://
    command: celery -A app.core.celery_app.celery_app flower
    depends_on:
      rabbitmq:
        condition: service_healthy
    ports:
      - "5555:5555"
    restart: unless-stopped

  viewer:
    build: ./viewer
    image: pdfdiff-turbo-viewer:1.1.7
    profiles:
      - prod
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
    restart: unless-stopped

  viewer-dev:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run start"
    environment:
      NG_CLI_CACHE_PATH: "/tmp/.angular"
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
      CHOKIDAR_INTERVAL: "1000"
    volumes:
      - ./viewer:/app
      - viewer_angular_cache:/app/.angular
      - /app/node_modules
    ports:
      - "4200:4200"
    restart: unless-stopped

  admin:
    build: ./admin
    image: pdfdiff-turbo-admin:1.1.7
    ports:
      - "8081:80"
      - "8444:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
    restart: unless-stopped

  admin-dev:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run start -- --port 4201"
    environment:
      NG_CLI_CACHE_PATH: "/tmp/.angular"
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
      CHOKIDAR_INTERVAL: "1000"
    volumes:
      - ./admin:/app
      - admin_angular_cache:/app/.angular
      - /app/node_modules
    ports:
      - "4201:4201"
    restart: unless-stopped

volumes:
  pgdata:
  data:
  viewer_angular_cache:
  admin_angular_cache:
